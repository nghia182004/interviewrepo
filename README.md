to run this

npm install express

npm install --save-dev jest 
(optional: 
add to package.json
"scripts": {
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js"
  }

  )

npm install mysql2

